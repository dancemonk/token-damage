{
  "name": "token-damage-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.6.0",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "lint": "eslint . && prettier --check .",
    "typecheck": "pnpm build && pnpm -r --if-present typecheck",
    "test": "pnpm build && pnpm -r --if-present test && vitest run --dir scripts",
    "check": "pnpm lint && pnpm typecheck && pnpm test",
    "format": "prettier --write .",
    "oracle": "pnpm -F @token-damage/core build && node scripts/oracle.mjs && node scripts/oracle.mjs --agent codex && node scripts/oracle.mjs --agent gemini && node scripts/oracle.mjs --agent opencode",
    "pool:age": "pnpm -F @token-damage/core build && node scripts/pool-age.mjs",
    "oracle:fixtures": "pnpm -F @token-damage/core build && node scripts/oracle.mjs --fixtures && node scripts/oracle.mjs --fixtures --agent codex && node scripts/oracle.mjs --fixtures --agent gemini && node scripts/oracle.mjs --fixtures --agent opencode",
    "oracle:live": "pnpm build && pnpm -F token-damage build && node scripts/oracle.mjs --live",
    "build": "pnpm -F @token-damage/core build",
    "build:site": "pnpm -F @token-damage/core build && pnpm -F @token-damage/web build"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^22.20.4",
    "eslint": "^10.11.0",
    "prettier": "^3.9.9",
    "typescript": "~6.0.3",
    "typescript-eslint": "^8.70.1",
    "vitest": "^5.0.1"
  }
}
