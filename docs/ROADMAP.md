# Roadmap

## MVP 0.1 — "The receipt printer"  (TASKS 1–9)
Claude Code only. Terminal receipt with guess and dispute. PNG. Fragment share link. Website with the hero and the
quiz. Ship when: oracle ≤1%, three developer friends laugh unprompted, share artifacts pass the leak test.

## 0.2 — Codex (TASKS 11)
Second launch. "Bilingual" achievement. Two-agents roast.

## V1 — identity
- Claude Code statusline: `⚠ today: Water Damage · $14 list · 3 interns` (`token-damage statusline`)
- Local git correlation (opt-in, reads `git log` in the session's cwd during the session's time window; labeled
  "vanity ratio"): tokens per commit, most expensive commit (4-char hash only), reverts, `Co-Authored-By` trailers
- "Ask the Adjuster" (aggregates → user's own `claude -p`; roast billed on the receipt)
- Monthly Statement (utility-bill layout, `design/`), December Wrapped
- Browser-only import (File System Access API) for people who distrust npx
- Local OTLP receiver (official telemetry, forward-only)
- Gemini CLI, Copilot CLI adapters on demand

## V2 — if people share it
- Menu-bar app (Tauri) with the "Server Room" ambient scene
- Cursor / OpenCode adapters; ChatGPT/Claude.ai export import (estimated tier)
- Discord bot rendering share links; README badge generated locally
- Team Statement (anti-leaderboard; separate brand on the shared engine)

## Keep, don't build
USB thermal printer that prints your daily damage at 6 PM · community-priced fake markets · NVML/RAPL power
metering for local models (the one place ESTIMATED becomes MEASURED) · MCP tool so the agent can see its own bill.

## Never
Public leaderboards · accounts required · composite efficiency score · notifications · streaks (except Touch Grass).
